## Packages
date-fns | Formatting relative dates and timestamps
framer-motion | Smooth animations for message bubbles and transitions
react-use-websocket | Robust WebSocket hook for real-time messaging

## Notes
- `useAuth` hook provides authentication state (user, isLoading, isAuthenticated, logout).
- WebSocket connects to the current host for real-time chat events.
- Using Shadcn sidebar patterns as explicitly requested.
- Tailwind config extend for `font-display` and `font-sans` is assumed or handles gracefully via CSS variables.
